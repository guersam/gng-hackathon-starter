# GNG Experiential Game Hackathon Starter 제품 명세

기준일: 2026-08-13

## 대상 환경

휴대전화와 데스크톱 웹 브라우저

## 제품의 목적

이 저장소의 제품은 하나의 게임이 아니라 체험형 시뮬레이션 게임을 위한 재사용 기준선이다. `simulation-kit`의 게임 정의와 manifest, `ui-foundation`의 게임 비의존 CSS base와 semantic theme token은 공유하되 팀·실시간·리더보드·성찰은 개별 게임이 선택한다. `cloudflare-host`의 멱등 명령 처리 유틸리티도 공유하지만, production Worker는 현재 요일 천재 전용이다. 외부 갤러리·팀 배정·투표는 시스템 경계 밖이다.

90분 해커톤에서는 7개 팀의 4~5명이 팀당 한 명의 AI 코더를 기다리지 않고 경험·규칙·플레이·피날레 패킷을 병렬로 만든다. 모든 게임 팩은 작은 플레이 루프와 30명·두 방 피날레 실행 계획을 가져야 한다.

### 기준 게임: 요일 천재

요일 천재는 3~5명이 한 팀이 되어 주기적인 공동 의무와 개인 문제 풀이를 동시에 다루는 실시간 시뮬레이션 게임이다. 목표는 최고 점수를 만드는 요령만 찾는 것이 아니라 제한된 주의, 위험 선택, 업무 분배와 회복이 팀에서 어떻게 나타나는지 플레이 기록을 근거로 대화하는 데 있다.

참가자는 자기 배포를 확인하기 위해 정확히 한 명이 참여하는 2분짜리 `혼자 연습`을 사용할 수 있다. 연습은 같은 시간·점수·재연결 규칙을 쓰지만 팀 시뮬레이션이나 경쟁 순위로 해석하지 않는다.

이 저장소는 해커톤 참가자가 자기 Cloudflare 계정에 미리 배포하고, 현장에서 규칙 하나를 안전하게 바꿔 다시 플레이할 수 있는 기준 구현이다. 대상 독자는 TypeScript와 웹 개발의 기본 경험이 있는 참가자다.

## 한 판의 계약

- 세션에는 최대 30명이 참가한다. 팀 정원은 팀마다 3~5명이며 시작 시 확정된다.
- 혼자 연습은 한 사람, 한 실행 단위, 120초로 고정되며 팀 세션과 섞이거나 리더보드에 표시되지 않는다.
- `팀별 시작`에서는 정원이 찬 팀의 구성원 누구나 준비를 선언해 그 팀의 5초 카운트다운을 시작한다. `전체 동시 시작`에서는 모든 팀의 준비가 갖춰졌을 때 전체가 함께 5초 카운트다운을 시작한다.
- 기본 플레이 시간은 8분이다. 연결이 끊기거나 화면이 백그라운드로 가도 시간은 흐른다. 세션 관리 화면에서 전체 일시정지, 재개와 종료를 할 수 있다.
- 팀 시계가 시작되면 9초마다 새 의무 구간이 열린다. 각 사람은 월요일부터 일요일까지 같은 순서를 따라 자기 버튼을 한 번 눌러야 한다.
- 구간 안에 누르지 않은 사람 한 명당 팀 크레딧이 1점 감소한다. 틀린 요일, 너무 이른 입력, 중복 입력은 의무를 충족하지 않지만 별도 벌점은 없다.
- 의무 구간은 문제를 푸는 동안에도 계속된다. 한 사람은 동시에 문제 하나만 풀 수 있고 요일 답 하나를 최종 제출한다.
- 날짜 범위의 기본 점수는 이번주 1, 이번달 2, 올해 5, 최근 3년 10, 최근 10년 20, 최근 100년 50, 최근 1000년 100이다.
- 제한시간은 3·6·9·12·15·18·21초다. 획득 점수는 기본 점수를 차례로 1·2·3·4·5·6·7로 나눈 값이다.
- 정답 점수는 오차 없이 합산하기 위해 1크레딧을 420 정수 단위로 저장한다. 화면에는 크레딧으로 표시한다.
- 오답 또는 시간 초과 벌점은 `max(10, floor(선택한 정답 점수 / 6) + 1)` 크레딧이다. 따라서 100점·3초 문제만 17점이고 나머지는 10점이다. 크레딧은 음수가 될 수 있다.
- 모든 팀의 원점수, 확정 인원으로 나눈 1인당 점수, 경과 시간과 상태가 실시간으로 보인다. 순위는 1인당 점수로 정하고 동점은 같은 순위다.

## 날짜와 시간의 계약

- 날짜 계산은 세션 시간대의 역산 그레고리력을 사용한다. 기본 시간대는 `Asia/Seoul`이다.
- 이번주·이번달·올해는 기준일이 속한 기간 전체를 뜻하므로 미래 날짜가 포함될 수 있다.
- 최근 N년은 팀 시작 때 고정한 기준일까지이며, 범위 안의 서수 날짜를 균등하게 뽑는다.
- 서버 도착 시각, 활성 플레이 시간, 클라이언트 표시 시각, 의무 구간과 문제 마감은 서로 구분한다. 판정 권한은 서버에만 있다.

## 세션 종료와 성찰

종료 화면은 점수만 설명하지 않는다. 의무 누락과 문제 풀이의 겹침, 위험 선택 변화, 마감 직전 회복, 구성원별 행동 분포를 시간순 기록으로 보여준다. 다음 세 질문을 기본으로 사용한다.

1. 팀의 주의가 가장 많이 갈라진 순간에 실제로 어떤 일이 있었나?
2. 위험과 보상에 대한 선택은 어떤 사건 뒤에 달라졌나?
3. 다음 판에서 규칙이나 역할 하나만 바꾼다면 무엇을 바꾸고, 기록에서 어떤 변화가 보이면 효과가 있었다고 말할 수 있나?

이 기록은 관찰 가능한 사건을 요약할 뿐 성격, 역량 또는 팀워크를 진단하지 않는다. 혼자 연습은 개인의 주의 변화와 실제 팀에 들어가기 전 정할 신호를 묻는다.

## 시스템 경계

- 브라우저는 표시와 입력을 담당하고 Worker 및 세션별 SQLite Durable Object가 인증, 상태, 마감, 저장과 방송을 권위 있게 처리한다.
- 이벤트 로그는 추가 전용이며 스냅샷으로 복구한다. 스키마와 프로토콜 버전은 `yoil-genius:v2`이다.
- 접속 링크에는 권한 토큰을 넣지 않는다. 참가 권한과 관리 권한은 분리하고 WebSocket의 첫 메시지에서 인증한다.
- 완료된 세션은 24시간 뒤 삭제하며, 그 전까지 세션 관리 화면에서 JSON 사건 기록을 내보낼 수 있다.
- 요일 천재가 보장하는 확장 지점은 점수 정책, 날짜 범위, 의무 주기 변형, 기록 투영, 밝은 테마 토큰이다. 다른 게임은 `GameManifest`와 `ServerGameDefinition` 계약을 구현하며 팀·실시간 기능을 선택적으로 사용한다.

## 완료 기준

새 Cloudflare 계정에서 별도 저장소나 비밀값을 수동 생성하지 않고 배포할 수 있어야 한다. 30개 클라이언트 부하 시험에서 중복 마감과 커밋된 사건 유실이 없어야 하며, 같은 사건 로그는 같은 최종 상태를 만들어야 한다. 모바일에서 핵심 의무·점수·문제 입력이 스크롤 없이 보이고 키보드, 모션 축소와 WCAG 2.2 AA 대비를 지원해야 한다.
